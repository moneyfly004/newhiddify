import 'package:flutter/material.dart';

/// 服务器列表页面（已集成到主页，此页面保留用于未来扩展）
class ServersPage extends StatelessWidget {
  const ServersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('服务器列表'),
    );
  }
}

